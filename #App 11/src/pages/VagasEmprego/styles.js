import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  titulo:{
    fontSize: 41,
    color: 'red',
    fontWeight: 'bold',
    textAlign: 'center',
    marginTop: 40,
    position: 'absolute',
    top: 0,
    left: 0,
    right: 0,
    zIndex: 1,
  },

  tela:{
    marginTop: 100
  },
})

export {styles}